const vacationSpots = ['Bali', 'Paris', 'Tulum'];

// Write your code below
for (let i = 0; i < vacationSpots.length; i++) {
  console.log('I would love to visit '+vacationSpots[i]);
}

// Arrays are a great way to process information stored in an array.  The loop condition should be tied to the length of the array.  i is the conventional variable for loop iterations.
