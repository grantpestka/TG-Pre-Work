let myName = 'Grant';
let myCity = 'Denver';
console.log(`My name is ${myName}. My favorite city is ${myCity}`);

//  A cleaner way to insert variables into strings is with template literals.  this are indicated by backticks and the variable is indicated with this syntas: ${variable}.
