let gainedDollar = 3;
let lostDollar = 50;

gainedDollar++;
lostDollar--;

// The increment operator (++) adds 1 to a variable. The decrement operator (--) subtracts 1 from a value.  Both reassign the new value to the variable.
