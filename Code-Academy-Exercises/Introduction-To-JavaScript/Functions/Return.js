function monitorCount(rows,columns) {
  return rows * columns;
}

const numOfMonitors = monitorCount(5,4);

console.log(numOfMonitors);

// The return keyword defines what is passed back from a function. A function stops evaluating when it hits the return keyword.
