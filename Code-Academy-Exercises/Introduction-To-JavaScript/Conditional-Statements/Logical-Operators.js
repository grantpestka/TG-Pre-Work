let mood = 'sleepy';
let tirednessLevel = 6;

if (mood === 'sleepy' && tirednessLevel > 8){
  console.log('time to sleep');
}
else{
  console.log('not bed time yet');
}

// The JavaScript AND opperator is &&.  The JavaScript OR operator is \\. The JavaScript NOT operator is ! (aka bang).  && returns true if both sides are true. \\ returns true if one side is true.  ! reverses the boolean value.
