const plantNeedsWater = day => day === 'Wednesday' ? true : false;


//Functions that only take one paramater do not need parentheses around that paramater.  Functions bodies that only have one line of code do not need curly brackets or the return keyword.
