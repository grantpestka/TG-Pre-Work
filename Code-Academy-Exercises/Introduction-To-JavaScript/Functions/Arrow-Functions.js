const plantNeedsWater = (day) => {
  if (day === 'Wednesday') {
    return true;
  } else {
    return false;
  }
}


// Arrow functions are created by defining a variable. Setting it equal to paramaters in parentheses with => pointing to the function body in curly brackets.
// const function_name = (param1, param2, ...) => {function_body}
